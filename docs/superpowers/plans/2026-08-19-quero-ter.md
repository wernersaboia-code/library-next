# Quero ter — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar a lista de desejados navegável com dezenas de livros (ordem alfabética, divisórias de letra, filtro, formulário recolhido) e fazer a capa baixar sozinha, conforme `docs/superpowers/specs/2026-08-19-quero-ter-design.md`.

**Architecture:** A mudança de servidor é mínima — dois timeouts e uma cláusula `ORDER BY`. O grosso é de cliente, e a parte com regra de negócio (normalizar acento, filtrar, agrupar por letra) sai de dentro do componente para um módulo puro `agrupar.ts`, que ganha testes automatizados de verdade. O componente `wishlist-client.tsx` (628 linhas, já grande) fica só com JSX e estado, sem crescer em lógica.

**Tech Stack:** Next.js 15 (App Router, Server Components), React 19, Drizzle ORM sobre Postgres (Supabase), Tailwind CSS, Vitest.

## Global Constraints

- Nomes de variável, comentários e mensagens de commit em **português**, prefixo `type:` minúsculo (`feat:`, `fix:`, `docs:`), no estilo do `git log` existente.
- **Nenhuma migração, coluna nova ou rota de API.** Só muda o `ORDER BY` de `fetchWishlist`.
- **Não usar `pnpm typecheck` / `pnpm lint` / `pnpm test` neste ambiente**: o wrapper de checagem de dependências do pnpm falha espuriamente. Chamar os binários direto: `./node_modules/.bin/tsc --noEmit`, `./node_modules/.bin/eslint . --ignore-pattern ".claude/**"`, `./node_modules/.bin/vitest run`.
- ESLint deste projeto proíbe **non-null assertion** (`@typescript-eslint/no-non-null-assertion: 'error'`) — nunca escrever `x!`.
- `tsconfig.json` **não** tem `noUncheckedIndexedAccess`: acesso indexado tipa como não-nulo, então índice ausente vira erro em execução, não de tipo. Todo acesso por chave dinâmica precisa de fallback explícito.
- Sem framework de teste de componente no repo: JSX é verificado por typecheck, lint e uso real no navegador. **Lógica pura vai para `agrupar.ts` justamente para ser testável.**
- **A ordenação acontece só no Postgres.** O cliente agrupa a lista já ordenada; nunca reordena. A collation do banco (`en_US.UTF-8`) já ordena acento corretamente — medido: `Alien | Ártico | Echo | Ícaro | Zumbi`.
- Servidor de desenvolvimento deste checkout: `http://localhost:3002`, já autenticado no painel do navegador. Navegar direto para a URL — **não** usar `preview_start` com nome de `launch.json`.

## Desvio da spec, deliberado

A spec (AD-3) pede divisórias de letra **fixas ao rolar** (`position: sticky`). Ao planejar, medi o conflito: `app/(main)/layout.tsx:28` já tem um cabeçalho `sticky top-0 z-10` com busca e navegação. Uma divisória também grudada em `top-0` ou fica **atrás** desse cabeçalho (invisível justamente quando gruda), ou exige um deslocamento fixo em pixels que quebra quando a altura do cabeçalho muda entre celular e desktop.

**Este plano entrega divisórias não fixas**, visualmente destacadas. O ganho que a spec buscava — marcos para saber onde se está ao rolar — vem da divisória em si; a fixação acrescentaria fragilidade contra um `sticky` que já existe. Registrado aqui para ser decisão consciente, não omissão.

---

### Task 1: Timeouts calibrados pela latência medida

**Files:**
- Modify: `lib/openlibrary.ts:8`
- Modify: `lib/covers.ts:44`

**Interfaces:**
- Consumes: nada.
- Produces: nada que outra tarefa importe. `TIMEOUT_MS` continua privado do módulo.

**Sobre não haver teste automatizado nesta tarefa:** o valor é uma constante de configuração. Um teste `expect(TIMEOUT_MS).toBe(15000)` reafirmaria o número sem provar comportamento, e passaria a quebrar em toda recalibração legítima. A prova real é de ponta a ponta, no navegador, na Task 5. Confirmado que nenhum teste existente fixa `5000`.

- [ ] **Step 1: Aumentar o timeout da busca externa**

Em `lib/openlibrary.ts`, substituir a linha 8:

```ts
const TIMEOUT_MS = 5000;
```

por:

```ts
// Medido contra a Open Library desta rede em 2026-08-19: mediana de 7 221 ms,
// pior amostra 8 899 ms, 3 de 5 buscas acima de 5 s. O limite anterior (5 s)
// disparava no uso normal e a busca "não funcionava" — 503 traduzido na tela
// como "preencha manualmente". 15 s é ~2x a mediana, com folga para variação.
const TIMEOUT_MS = 15000;
```

- [ ] **Step 2: Aumentar o timeout do download da capa**

Em `lib/covers.ts`, dentro de `fetchOpenLibraryCover`, substituir:

```ts
  const res = await fetch(url, { signal: AbortSignal.timeout(10000) });
```

por:

```ts
  // A rota completa de capa levou 11 741 ms na medição de 2026-08-19, contra
  // o limite anterior de 10 s — por isso nenhum dos livros vindos da busca
  // tinha capa. 20 s dá ~1,7x a chamada observada.
  const res = await fetch(url, { signal: AbortSignal.timeout(20000) });
```

- [ ] **Step 3: Typecheck e lint**

Run: `./node_modules/.bin/tsc --noEmit`
Expected: sem saída.

Run: `./node_modules/.bin/eslint . --ignore-pattern ".claude/**"`
Expected: `✖ 2 problems (0 errors, 2 warnings)` — os dois avisos pré-existentes em `next.config.ts` e `postcss.config.mjs`. Zero erros.

- [ ] **Step 4: Confirmar que a suíte da Open Library segue verde**

Run: `./node_modules/.bin/vitest run test/openlibrary.test.ts`
Expected: PASS. Esses testes mockam `fetch`, então não dependem do valor do timeout — a passagem confirma que a mudança não quebrou o contrato do módulo.

- [ ] **Step 5: Commit**

```bash
git add lib/openlibrary.ts lib/covers.ts
git commit -m "fix: calibra timeouts da Open Library pela latencia medida"
```

---

### Task 2: `fetchWishlist` ordena por título

**Files:**
- Modify: `lib/db/queries.ts` (a cláusula `.orderBy` de `fetchWishlist`)
- Test: `test/db/wishlist.test.ts` (acrescentar ao arquivo existente)

**Interfaces:**
- Consumes: nada.
- Produces: `fetchWishlist(userId)` passa a devolver a lista **ordenada por título**. A Task 4 depende disso: ela agrupa por letra sem reordenar, o que só funciona se a lista já vier ordenada.

- [ ] **Step 1: Escrever o teste que falha**

Em `test/db/wishlist.test.ts`, acrescentar este bloco no **final do arquivo**, depois do `describe('fetchWishlist', ...)` existente (não mexer nos testes que já estão lá):

```ts
describe('ordem da lista de desejados', () => {
  it('ordena por título, não por data de cadastro', async () => {
    const [u] = await ctx.sql`
      insert into app_users (email) values ('ordem@x.com') returning id`;
    // Inseridos fora de ordem alfabética de propósito: com o ORDER BY antigo
    // (created_at) sairiam nesta mesma ordem, e o teste passaria por engano.
    const titulosInseridos = ['Zumbi', 'Alien', 'Ártico', '100 anos', 'Echo'];
    for (const t of titulosInseridos) {
      await ctx.sql`
        insert into books (user_id, title, title_source, source, owned)
        values (${u.id}, ${t}, ${t}, 'manual', false)`;
    }

    const { fetchWishlist } = await import('@/lib/db/queries');
    const todos = (await fetchWishlist(u.id)).map((r) => r.title);

    // Filtra para só os títulos que ESTA inserção acabou de criar, em vez de
    // comparar o array inteiro: `fetchWishlist` não filtra por usuário no
    // banco de teste (a suíte conecta como `postgres`, que ignora RLS — o
    // mesmo mecanismo que test/db/rls.test.ts existe para provar de verdade,
    // via SET LOCAL ROLE). `todos` inclui então os livros de OUTROS testes
    // deste arquivo, que rodam antes deste no mesmo schema. Como o filtro
    // preserva a ordem relativa do array original, e a ordenação continua
    // sendo feita só pelo `ORDER BY title` do Postgres, isolar por conteúdo
    // prova a ordenação sem depender de isolamento por usuário.
    const titulos = todos.filter((t) => titulosInseridos.includes(t));

    // Acento não desloca o item para o fim: a collation do banco põe "Á"
    // logo depois de "A". Números vêm antes das letras — é onde o Postgres
    // os põe, e o agrupamento do cliente segue essa ordem em vez de brigar
    // com ela.
    expect(titulos).toEqual(['100 anos', 'Alien', 'Ártico', 'Echo', 'Zumbi']);
  });
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `./node_modules/.bin/vitest run test/db/wishlist.test.ts`
Expected: FAIL no teste novo — a ordem devolvida é a de inserção (`['Zumbi', 'Alien', 'Ártico', '100 anos', 'Echo']`), porque `fetchWishlist` ainda ordena por `createdAt`. Os 6 testes que já existiam continuam passando.

- [ ] **Step 3: Trocar a ordenação**

Em `lib/db/queries.ts`, dentro de `fetchWishlist`, substituir:

```ts
      .orderBy(books.createdAt)
```

por:

```ts
      // Alfabética, não cronológica: a lista é para procurar um título
      // específico, não para ver o que entrou por último. A ordenação fica
      // só aqui — o cliente agrupa por letra a partir desta ordem, sem
      // reordenar, para não existirem duas ordens que possam divergir.
      .orderBy(books.title)
```

- [ ] **Step 4: Rodar para confirmar que passa**

Run: `./node_modules/.bin/vitest run test/db/wishlist.test.ts`
Expected: PASS — 7 testes (os 6 anteriores mais o novo).

- [ ] **Step 5: Typecheck e lint**

Run: `./node_modules/.bin/tsc --noEmit`
Expected: sem saída.

Run: `./node_modules/.bin/eslint . --ignore-pattern ".claude/**"`
Expected: `✖ 2 problems (0 errors, 2 warnings)`.

- [ ] **Step 6: Commit**

```bash
git add lib/db/queries.ts test/db/wishlist.test.ts
git commit -m "feat: lista de desejados em ordem alfabetica"
```

---

### Task 3: Módulo puro de filtro e agrupamento

**Files:**
- Create: `app/(main)/desejados/agrupar.ts`
- Test: `test/desejados-agrupar.test.ts` (criar)

**Interfaces:**
- Consumes: nada.
- Produces — a Task 4 importa exatamente isto:
  - `export interface LivroFiltravel { title: string; authors: string[] | null }`
  - `export function normalizar(texto: string): string`
  - `export function filtrarLivros<T extends LivroFiltravel>(livros: T[], termo: string): T[]`
  - `export function letraInicial(title: string): string`
  - `export function agruparPorLetra<T extends LivroFiltravel>(livros: T[]): { letra: string; livros: T[] }[]`

  Os genéricos existem para o módulo não importar `LivroDesejado` do componente: ele só exige `title` e `authors`, e devolve os objetos originais intactos.

**Por que este módulo existe:** o repo não tem teste de componente, então qualquer lógica dentro do `.tsx` fica sem cobertura automatizada. Normalizar acento, casar termos e agrupar por letra são justamente as partes com casos de borda (acento, número, título vazio). Fora do JSX, viram funções puras testáveis.

- [ ] **Step 1: Escrever os testes que falham**

Criar `test/desejados-agrupar.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import {
  agruparPorLetra, filtrarLivros, letraInicial, normalizar,
} from '@/app/(main)/desejados/agrupar';

const livro = (title: string, authors: string[] | null = []) => ({ title, authors });

describe('normalizar', () => {
  it('tira acento e caixa', () => {
    expect(normalizar('Ficção Científica')).toBe('ficcao cientifica');
    expect(normalizar('ÁRTICO')).toBe('artico');
  });

  it('não quebra com string vazia', () => {
    expect(normalizar('')).toBe('');
  });
});

describe('filtrarLivros', () => {
  const livros = [
    livro('O Iluminado', ['Stephen King']),
    livro('Ficção Científica', null),
    livro('Duna', ['Frank Herbert']),
  ];

  it('termo vazio devolve tudo, sem copiar em vão', () => {
    expect(filtrarLivros(livros, '')).toEqual(livros);
    expect(filtrarLivros(livros, '   ')).toEqual(livros);
  });

  it('casa trecho do título, ignorando caixa', () => {
    expect(filtrarLivros(livros, 'ILUMIN').map((l) => l.title)).toEqual([
      'O Iluminado',
    ]);
  });

  it('casa o autor, não só o título', () => {
    expect(filtrarLivros(livros, 'herbert').map((l) => l.title)).toEqual(['Duna']);
  });

  it('ignora acento nos dois sentidos', () => {
    expect(filtrarLivros(livros, 'ficcao').map((l) => l.title)).toEqual([
      'Ficção Científica',
    ]);
    expect(filtrarLivros(livros, 'ficção').map((l) => l.title)).toEqual([
      'Ficção Científica',
    ]);
  });

  it('não quebra quando o livro não tem autor', () => {
    expect(filtrarLivros(livros, 'cientifica')).toHaveLength(1);
  });

  it('devolve vazio quando nada casa', () => {
    expect(filtrarLivros(livros, 'zzz-inexistente')).toEqual([]);
  });
});

describe('letraInicial', () => {
  it('devolve a letra, sem acento e em maiúscula', () => {
    expect(letraInicial('duna')).toBe('D');
    expect(letraInicial('Ártico')).toBe('A');
  });

  it('agrupa número e símbolo sob #', () => {
    expect(letraInicial('100 anos')).toBe('#');
    expect(letraInicial('...e não sobrou nenhum')).toBe('#');
  });

  it('título vazio cai em # em vez de quebrar', () => {
    expect(letraInicial('')).toBe('#');
  });
});

describe('agruparPorLetra', () => {
  it('agrupa preservando a ordem recebida, sem reordenar', () => {
    // Esta é a ordem que o Postgres devolve (número antes de letra, acento
    // junto da letra-base). A função NÃO pode reordenar: existe uma ordem só.
    const secoes = agruparPorLetra([
      livro('100 anos'), livro('Alien'), livro('Ártico'),
      livro('Echo'), livro('Zumbi'),
    ]);
    expect(secoes.map((s) => s.letra)).toEqual(['#', 'A', 'E', 'Z']);
    expect(secoes[1].livros.map((l) => l.title)).toEqual(['Alien', 'Ártico']);
  });

  it('lista vazia devolve nenhuma seção', () => {
    expect(agruparPorLetra([])).toEqual([]);
  });

  it('uma seção só quando todos começam com a mesma letra', () => {
    const secoes = agruparPorLetra([livro('Alien'), livro('Ártico')]);
    expect(secoes).toHaveLength(1);
    expect(secoes[0].letra).toBe('A');
  });
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `./node_modules/.bin/vitest run test/desejados-agrupar.test.ts`
Expected: FAIL — o módulo `agrupar.ts` não existe, então a importação quebra antes de qualquer asserção rodar.

- [ ] **Step 3: Escrever o módulo**

Criar `app/(main)/desejados/agrupar.ts`:

```ts
/**
 * Filtro e agrupamento da lista de desejados.
 *
 * Vive fora do componente de propósito: o repo não tem teste de componente,
 * então lógica dentro do .tsx ficaria sem cobertura. Aqui são funções puras,
 * testadas em test/desejados-agrupar.test.ts.
 */

/** O mínimo que estas funções exigem de um livro. */
export interface LivroFiltravel {
  title: string;
  authors: string[] | null;
}

/**
 * Caixa baixa e sem acento, para comparar texto digitado com texto gravado.
 * NFD separa a letra do acento; o intervalo BACKSLASH-u-0300 a BACKSLASH-u-036f
 * (escrito por extenso aqui de propósito) é o bloco Unicode das marcas
 * diacríticas assim separadas, que então descartamos no regex abaixo. Os
 * caracteres literais correspondentes são invisíveis e se corrompem ao
 * copiar — por isso o regex usa a forma escapada, nunca o caractere direto.
 */
export function normalizar(texto: string): string {
  return texto
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .toLowerCase();
}

/**
 * Livros cujo título OU autor contém o termo. Termo vazio devolve o próprio
 * array recebido — sem cópia, porque o caso comum é não haver filtro.
 */
export function filtrarLivros<T extends LivroFiltravel>(
  livros: T[],
  termo: string
): T[] {
  const alvo = normalizar(termo.trim());
  if (!alvo) return livros;

  return livros.filter((livro) => {
    const campos = [livro.title, ...(livro.authors ?? [])];
    return campos.some((campo) => normalizar(campo).includes(alvo));
  });
}

/**
 * A letra da divisória. Qualquer coisa que não seja A–Z depois de tirar o
 * acento (número, símbolo, título vazio) cai em '#'.
 */
export function letraInicial(title: string): string {
  const primeira = normalizar(title).trim().charAt(0).toUpperCase();
  return primeira >= 'A' && primeira <= 'Z' ? primeira : '#';
}

/**
 * Quebra a lista em seções por letra inicial.
 *
 * **Preserva a ordem recebida.** A ordenação é do Postgres (ver `fetchWishlist`);
 * reordenar aqui criaria uma segunda ordem que poderia divergir da primeira.
 * Por isso a seção '#' aparece onde o banco a põe — no começo, já que números
 * ordenam antes de letras.
 */
export function agruparPorLetra<T extends LivroFiltravel>(
  livros: T[]
): { letra: string; livros: T[] }[] {
  const secoes: { letra: string; livros: T[] }[] = [];

  for (const livro of livros) {
    const letra = letraInicial(livro.title);
    const ultima = secoes[secoes.length - 1];
    if (ultima && ultima.letra === letra) ultima.livros.push(livro);
    else secoes.push({ letra, livros: [livro] });
  }

  return secoes;
}
```

- [ ] **Step 4: Rodar para confirmar que passa**

Run: `./node_modules/.bin/vitest run test/desejados-agrupar.test.ts`
Expected: PASS — 14 testes.

- [ ] **Step 5: Typecheck e lint**

Run: `./node_modules/.bin/tsc --noEmit`
Expected: sem saída.

Run: `./node_modules/.bin/eslint . --ignore-pattern ".claude/**"`
Expected: `✖ 2 problems (0 errors, 2 warnings)`.

- [ ] **Step 6: Commit**

```bash
git add "app/(main)/desejados/agrupar.ts" test/desejados-agrupar.test.ts
git commit -m "feat: modulo puro de filtro e agrupamento dos desejados"
```

---

### Task 4: Interface — filtro, divisórias e formulário recolhido

**Files:**
- Modify: `app/(main)/desejados/wishlist-client.tsx`

**Interfaces:**
- Consumes, da Task 3: `filtrarLivros(livros, termo)` e `agruparPorLetra(livros)` de `./agrupar`. `LivroDesejado` (já definido neste arquivo) satisfaz `LivroFiltravel`, porque tem `title: string` e `authors: string[] | null`.
- Consumes, da Task 2: `initial` já chega **ordenado por título**.
- Produces: nada que outra tarefa importe.

- [ ] **Step 1: Importar o módulo e criar o estado do filtro**

Em `app/(main)/desejados/wishlist-client.tsx`, logo depois da linha:

```ts
import { EmptyState } from '@/components/empty-state';
```

acrescentar:

```ts
import { agruparPorLetra, filtrarLivros } from './agrupar';
```

Depois, dentro de `WishlistClient`, logo abaixo da linha `const [removingId, setRemovingId] = useState<number | null>(null);`, acrescentar:

```ts
  // Filtro só no cliente: a lista inteira já veio numa resposta só, e a
  // escala é de dezenas de livros. Sem ida ao servidor, sem estado na URL.
  const [filtro, setFiltro] = useState('');
```

E logo **antes** do `return (` do componente, acrescentar:

```ts
  const visiveis = filtrarLivros(initial, filtro);
  const secoes = agruparPorLetra(visiveis);
  const filtrando = filtro.trim() !== '';
  const plural = (n: number) => (n === 1 ? 'livro' : 'livros');
```

- [ ] **Step 2: Recolher o bloco de cadastro num `<details>`**

Ainda em `wishlist-client.tsx`, no JSX, substituir a linha de abertura do bloco de cadastro:

```tsx
      <div className="border rounded-md p-4 space-y-3">
        <h2 className="text-lg font-semibold">Adicionar à lista</h2>
```

por:

```tsx
      {/* Recolhido por padrão: quem chega aqui quase sempre quer VER a lista,
          e este bloco ocupava a primeira tela inteira. `<details>` nativo em
          vez de mais um useState — já são doze neste componente — e vem com
          teclado e leitor de tela de graça. */}
      <details className="border rounded-md p-4">
        <summary className="cursor-pointer text-lg font-semibold">
          Adicionar livro
        </summary>
        <div className="mt-3 space-y-3">
```

E o fechamento correspondente. Localizar o fim desse bloco — as linhas:

```tsx
        {erro && <p className="text-sm text-red-600">{erro}</p>}
        {aviso && <p className="text-sm text-amber-600">{aviso}</p>}
      </div>
```

e substituir por:

```tsx
        {erro && <p className="text-sm text-red-600">{erro}</p>}
        {aviso && <p className="text-sm text-amber-600">{aviso}</p>}
        </div>
      </details>
```

- [ ] **Step 3: Trocar a lista pelo filtro + seções**

Substituir todo o bloco que hoje renderiza a lista — de `{initial.length === 0 ? (` até o `)}` que fecha o ternário, isto é:

```tsx
      {initial.length === 0 ? (
        <EmptyState
          icone={BookmarkPlusIcon}
          titulo="Nenhum livro na lista de desejados"
          descricao="Busque na Open Library ou preencha manualmente acima."
        />
      ) : (
        <ul className="space-y-3">
          {initial.map((livro) => (
            <ItemDesejado
              key={livro.id}
              livro={livro}
              removendo={removingId === livro.id}
              onApagar={() => void apagar(livro)}
            />
          ))}
        </ul>
      )}
```

por:

```tsx
      {initial.length === 0 ? (
        <EmptyState
          icone={BookmarkPlusIcon}
          titulo="Nenhum livro na lista de desejados"
          descricao="Busque na Open Library ou preencha manualmente acima."
        />
      ) : (
        <div className="space-y-3">
          <div className="space-y-1">
            <Label className="sr-only" htmlFor="wishlist-filtro">
              Filtrar a lista
            </Label>
            <Input
              id="wishlist-filtro"
              type="search"
              value={filtro}
              onChange={(e) => setFiltro(e.target.value)}
              placeholder="Filtrar por título ou autor..."
            />
            {/* O contador acompanha o filtro: um número fixo contradiria o
                que está na tela assim que a lista fosse recortada. */}
            <p className="text-sm text-muted-foreground">
              {filtrando
                ? `${visiveis.length} de ${initial.length} ${plural(initial.length)}`
                : `${initial.length} ${plural(initial.length)}`}
            </p>
          </div>

          {visiveis.length === 0 ? (
            // Estado próprio: o EmptyState de lista vazia diria a coisa
            // errada — a lista tem livros, o filtro é que não achou nenhum.
            <div className="rounded-md border border-dashed p-6 text-center">
              <p className="text-sm text-muted-foreground">
                Nenhum livro casa com “{filtro.trim()}”.
              </p>
              <Button
                type="button"
                variant="outline"
                size="sm"
                className="mt-3"
                onClick={() => setFiltro('')}
              >
                Limpar filtro
              </Button>
            </div>
          ) : (
            <div className="space-y-5">
              {secoes.map((secao) => (
                <section key={secao.letra}>
                  <h2 className="mb-2 border-b pb-1 font-display text-sm font-semibold text-muted-foreground">
                    {secao.letra}
                  </h2>
                  <ul className="space-y-3">
                    {secao.livros.map((livro) => (
                      <ItemDesejado
                        key={livro.id}
                        livro={livro}
                        removendo={removingId === livro.id}
                        onApagar={() => void apagar(livro)}
                      />
                    ))}
                  </ul>
                </section>
              ))}
            </div>
          )}
        </div>
      )}
```

- [ ] **Step 4: Typecheck e lint**

Run: `./node_modules/.bin/tsc --noEmit`
Expected: sem saída.

Run: `./node_modules/.bin/eslint . --ignore-pattern ".claude/**"`
Expected: `✖ 2 problems (0 errors, 2 warnings)`. Em particular, nenhum aviso de import não usado: `Label`, `Input` e `Button` já eram importados neste arquivo e continuam em uso.

- [ ] **Step 5: Verificar no navegador**

Servidor em `http://localhost:3002`, sessão já autenticada no painel. Navegar direto para `http://localhost:3002/desejados`. Conferir:

- O formulário aparece **fechado**, como "Adicionar livro", e a lista começa logo abaixo sem precisar rolar. Clicar abre; clicar de novo fecha.
- A lista aparece em seções por letra, com o cabeçalho da letra visível, e os títulos em ordem alfabética dentro de cada seção. Conferir por DOM, não só a olho:
  ```js
  [...document.querySelectorAll('section > h2')].map(h => h.innerText)
  ```
- O contador mostra `26 livros` sem filtro.
- Digitar `pen` no filtro: a lista reduz, o contador vira `N de 26 livros`, e as seções são recalculadas. Usar clique/entrada real via DOM:
  ```js
  const i = document.querySelector('#wishlist-filtro');
  const set = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set;
  set.call(i, 'pen'); i.dispatchEvent(new Event('input', { bubbles: true }));
  ```
- Digitar `zzz-inexistente`: aparece a mensagem "Nenhum livro casa com…" e o botão **Limpar filtro** — e **não** o estado vazio "Nenhum livro na lista de desejados". Clicar em Limpar filtro restaura a lista inteira.
- Filtrar por um autor (ex.: `king`) traz o livro mesmo com o termo não estando no título.
- Redimensionar para 375×812: filtro, divisórias e cartões cabem, sem rolagem horizontal (`document.documentElement.scrollWidth === document.documentElement.clientWidth`).

- [ ] **Step 6: Commit**

```bash
git add "app/(main)/desejados/wishlist-client.tsx"
git commit -m "feat: filtro, divisorias por letra e cadastro recolhido nos desejados"
```

---

### Task 5: Verificação final, incluindo a capa de ponta a ponta

**Files:** nenhum — só verificação.

**Interfaces:** N/A.

- [ ] **Step 1: Suíte completa**

Run: `./node_modules/.bin/vitest run`
Expected: todos os arquivos passam. A base antes desta feature era 43 arquivos / 293 testes; com o teste novo da Task 2 (+1) e os 14 da Task 3 num arquivo novo, esperar **44 arquivos / 308 testes**. Nenhuma falha.

- [ ] **Step 2: Typecheck e lint com tudo integrado**

Run: `./node_modules/.bin/tsc --noEmit`
Expected: sem saída.

Run: `./node_modules/.bin/eslint . --ignore-pattern ".claude/**"`
Expected: `✖ 2 problems (0 errors, 2 warnings)`.

- [ ] **Step 3: A prova do AD-1 — a capa chega sozinha**

Este é o cenário que falhava. Em `http://localhost:3002/desejados`:

1. Abrir "Adicionar livro".
2. Buscar por um título com capa conhecida na Open Library — `penpal` serve (verificado: tem `cover_i`).
3. A busca deve **retornar resultados**, não a mensagem "Não foi possível buscar agora". Ela pode demorar vários segundos; isso é esperado e o botão mostra "Buscando...".
4. Clicar em **Escolher** no resultado certo.
5. O livro entra na lista **com a capa**, sem nenhuma ação extra e sem o aviso "Livro adicionado, mas a capa não pôde ser baixada".

Confirmar a capa pelo DOM, não pela aparência:
```js
[...document.querySelectorAll('img')].filter(i => i.src.includes('/storage/')).length
```
Esperado: maior que zero.

6. **Limpar o teste:** apagar da lista o livro que foi adicionado para esta verificação, pelo botão de apagar do próprio cartão — a menos que seja um livro que o dono queira mesmo manter.

- [ ] **Step 4: Confirmar que o resto não mudou**

- `/favoritos`, `/proximos` e `/leituras` seguem iguais — nenhum deles usa `fetchWishlist` nem o módulo novo.
- O Acervo continua buscando e paginando normalmente.
- Apagar um livro da lista de desejados continua pedindo confirmação quando ele tem notas.

- [ ] **Step 5: Commit final, se algo precisou de ajuste**

Se a verificação não pedir mudança nenhuma, não há o que commitar — esta tarefa trava a qualidade, não gera commit.
